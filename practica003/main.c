#include <stdio.h>
#include <stdlib.h>

int main()
{
    system("cls");
    int nume=0;
    printf("Write a number to choose the options\n");
    scanf("%d",&nume);
    switch(nume)
    {
        nume 1:
        system("cls");
        break;
    }
    /*
    for(numeros=2;numeros>0;numeros--);
    {
      printf("Write a number or die do it:\n");
      scanf("%d",&numero);
      suma=suma+numero;

      system("cls");
    system("pause");
    }
    resultado=(suma);
    printf("%f",resultado);
    */
    return 0;
}
